# Training-only deps for lighter models (Colab / GPU VM).
# Does not replace requirements.txt used by the FastAPI backend.

transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.13.0
trl>=0.9.0
pyyaml>=6.0
# Optional 4-bit QLoRA (set use_qlora: true in config_qwen05.yaml)
bitsandbytes>=0.43.0
